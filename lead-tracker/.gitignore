node_modules/
.next/
# Generated data (large; Overture is CC-BY — don't republish the raw set).
# Everything under data/ is build output (DB, parquet, CSV/JSONL exports,
# call sheets, the data dictionary) — regenerated by the scripts, never committed.
data/
